{
  "name": "carbon-credit-blockchain",
  "version": "1.0.0",
  "description": "Blockchain module for carbon credit tracking in agri-platform",
  "scripts": {
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}

